
class Out:
    
	def __init__(self, affectation):
		self.a = affectation

	def writeFile():
		f = open('output', 'w')
		for i in self.a.row:
			line = "{} {} {}/n".format(a[i].rang, a[i].place, a[i].group)
			f.write(line)

	def writeTest():
		f = open('output', 'w')
		for s in range(10):
			line = "{} {} {}/n".format("s.rang", "s.place", "s.group")
			f.write(line)